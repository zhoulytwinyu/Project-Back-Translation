CREATE TABLE Peptide (
  PID INTEGER PRIMARY KEY AUTOINCREMENT,
  sequence VARCHAR(2048) NOT NULL UNIQUE,
);

CREATE TABLE Nucleotide (
  NID INTEGER PRIMARY KEY AUTOINCREMENT,
  sequence VARCHAR(2048) NOT NULL UNIQUE,
  PID INTEGER,
  FOREIGN KEY(PID) REFERENCES Peptide(PID)
);

